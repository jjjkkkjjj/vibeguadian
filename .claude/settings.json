{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.rs$' && /Users/junnosuke.kado/.cargo/bin/rustfmt \"$f\"; } 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "rustfmt..."
          }
        ]
      }
    ]
  }
}
